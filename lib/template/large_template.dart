import 'package:flutter/material.dart';

class LargeTemplate extends StatelessWidget {
  const LargeTemplate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}
