import 'package:flutter/material.dart';

class MediumAboutMePage extends StatelessWidget {
  const MediumAboutMePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
