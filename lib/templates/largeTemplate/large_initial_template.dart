import 'package:flutter/material.dart';

class LargeInitialTemplate extends StatelessWidget {
  const LargeInitialTemplate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
